# CodeSpace

CodeSpace is a Windows desktop app that helps you organize your VS Code workspaces,
launch them, and run their tasks — all from one place.

## Workspaces

- Automatically finds all the VS Code workspaces on your computer
- Opens a workspace in VS Code with one click
- Brings an open workspace to the front; click again to minimize it
- Lets you give each workspace its own color, so you can tell them apart at a glance
- Create a new workspace by simply dragging a folder into the app

## Tasks

- Run the tasks of your projects directly from the app, in a built-in terminal
- Each workspace has its own set of tabs, with colored output for successes, warnings and errors
- Tasks that start a local server open the right page in your browser automatically
- Create ready-made sample tasks with the "New task" button — no manual setup needed
- Tasks whose files are missing are shown grayed out and cannot be started
- Tasks can be deleted from the list; tasks that change important data ask for confirmation first

## Vault

- Keep the credentials of each project in one place: logins, FTP accounts,
  API tokens, cards, secure notes and custom entries
- The vault is unlocked with Windows Hello — your PIN, fingerprint or face,
  with no extra password to remember
- Credentials are encrypted on your computer and never leave it
- Each workspace has its own separate vault
- Copy any field or note to the clipboard with one click
- Lock the vault again whenever you want, straight from the toolbar
- Export all your credentials to a single file and import them back on another computer

## Built-in extras

- A dedicated CodeSpace folder in your projects for the files generated by the app,
  with a safety prompt before deleting it
- Enable the "agents & instructions" folder for a workspace with one toggle
- The app checks for new versions and updates itself automatically

## Download

The latest version is always available in the [Releases](https://github.com/Davide-Scarsi/CodeSpace/releases)
section of this repository:

- `CodeSpace_Setup.exe` — installer (recommended)
- `CodeSpace.exe` — portable version, no installation needed

> This repository contains official releases only.
